let nome = "Hugo Leao Portal";

for (let indice = 0; indice <= 1; indice++) {
  alert(`Indice = ${indice}`);
}

for (let i = 0; i < nome.length; i++) {
  if (nome[i] != " ") {
    console.log(nome[i]);
  }
}
