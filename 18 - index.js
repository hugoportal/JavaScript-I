const idade = prompt("Informe a sua idade:");
/*
if (idade > 18) {
  alert("Você é maior de idade");
} else if (idade > 12) {
  alert("Você é menor de idade");
} else {
  alert("Você é uma criança");
}
*/

/* --- Operador ternário ---
const resultado = condição ? resultado_verdadeiro : resultado_false
*/

const resultado = idade > 18 ? alert("Você é maior") : alert("Você é menor");
const result = 6 === 7 ? "Verdadeiro" : "False";
console.log(result);

//---------------------------------------
